import { Environment } from '@mockoon/commons';
/**
 * Load and parse one or more JSON data file(s).
 *
 * @param filePaths
 */
export declare const parseDataFiles: (filePaths: string[], userOptions?: {
    ports: number[];
    hostnames: string[];
}, repair?: boolean) => Promise<{
    originalPath: string;
    environment: Environment;
}[]>;
