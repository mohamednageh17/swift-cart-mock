import { fromSafeString } from '../utils';
export const systemHelperNames = [
    'getEnvVar'
];
export const SystemHelpers = function (options) {
    return {
        // get environment variable
        getEnvVar: function (...args) {
            var _a, _b;
            // remove last item (handlebars options argument)
            const parameters = args.slice(0, -1);
            if (parameters.length <= 0) {
                return;
            }
            let varName = String(fromSafeString(parameters[0]));
            const defaultValue = (_a = fromSafeString(parameters[1])) !== null && _a !== void 0 ? _a : '';
            if (!varName.startsWith(options.prefix)) {
                varName = options.prefix + varName;
            }
            return (_b = process.env[varName]) !== null && _b !== void 0 ? _b : defaultValue;
        }
    };
};
//# sourceMappingURL=system-helpers.js.map