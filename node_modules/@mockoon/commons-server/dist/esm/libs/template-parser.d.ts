import { Environment, ProcessedDatabucket } from '@mockoon/commons';
import { Request, Response } from 'express';
/**
 * Parse a content with Handlebars
 * @param shouldOmitDataHelper
 * @param content
 * @param environment
 * @param processedDatabuckets
 * @param request
 */
export declare const TemplateParser: ({ shouldOmitDataHelper, content, environment, processedDatabuckets, globalVariables, request, response, envVarsPrefix }: {
    shouldOmitDataHelper: boolean;
    content: string;
    environment: Environment;
    processedDatabuckets: ProcessedDatabucket[];
    globalVariables: Record<string, any>;
    request?: Request;
    response?: Response;
    envVarsPrefix: string;
}) => string;
