import { Request } from 'express';
export declare const FILTERS: {
    eq: (data: any, query: string) => boolean;
    ne: (data: any, query: string) => boolean;
    gt: (data: any, query: string) => boolean;
    gte: (data: any, query: string) => boolean;
    lt: (data: any, query: string) => boolean;
    lte: (data: any, query: string) => boolean;
    like: (data: any, query: string) => boolean;
    start: (data: any, query: string) => boolean;
    end: (data: any, query: string) => boolean;
};
export declare function parseFilters(queryParams: Request['query']): FilterData[];
export declare function applyFilter(data: any, filterData: FilterData): boolean;
export type Filter = keyof typeof FILTERS;
export type FilterData = {
    path: string;
    filter: Filter;
    value: string;
};
