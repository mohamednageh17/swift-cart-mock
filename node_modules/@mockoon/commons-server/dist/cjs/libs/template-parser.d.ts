import { Environment, ProcessedDatabucket } from '@mockoon/commons';
import { Response } from 'express';
import { IncomingMessage } from 'http';
import { ServerRequest } from './requests';
export type WebSocketRequest = {
    message?: string;
    request?: IncomingMessage;
};
/**
 * Parse a content with Handlebars
 * @param shouldOmitDataHelper
 * @param content
 * @param environment
 * @param processedDatabuckets
 * @param request
 *
 * @throws {Error}
 */
export declare const TemplateParser: ({ shouldOmitDataHelper, content, environment, processedDatabuckets, globalVariables, request, response, envVarsPrefix, publicBaseUrl }: {
    shouldOmitDataHelper: boolean;
    content: string;
    environment: Environment;
    processedDatabuckets: ProcessedDatabucket[];
    globalVariables: Record<string, any>;
    request?: ServerRequest;
    response?: Response;
    envVarsPrefix: string;
    publicBaseUrl?: string;
}) => string;
