import type { crudRoutesBuilder } from '@mockoon/commons';
import { ProcessedDatabucket, RouteResponse } from '@mockoon/commons';
import { Request, Response } from 'express';
export declare const crudRouteParamName = "id";
/**
 * Creates a set of CRUD actions for a given databucket type
 */
export declare const databucketActions: (crudId: ReturnType<typeof crudRoutesBuilder>[number]["id"], databucket: ProcessedDatabucket, request: Request, response: Response, routeCrudKey: RouteResponse["crudKey"]) => any;
