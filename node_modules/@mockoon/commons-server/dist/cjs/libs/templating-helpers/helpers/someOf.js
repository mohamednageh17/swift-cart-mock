"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// return one random item
// return some random item as an array (to be used in triple braces) or as a string
const commons_1 = require("@mockoon/commons");
const someOf = function (itemList, min, max, stringify = false) {
    const randomItems = itemList
        .sort(() => 0.5 - Math.random())
        .slice(0, (0, commons_1.RandomInt)(min, max));
    if (stringify === true) {
        return JSON.stringify(randomItems);
    }
    return randomItems;
};
exports.default = someOf;
//# sourceMappingURL=someOf.js.map