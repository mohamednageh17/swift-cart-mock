"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const date_fns_1 = require("date-fns");
const faker_1 = require("../../faker");
const time = function (...args) {
    let format;
    const options = {
        from: '1970-01-01T00:00:00',
        to: '1970-01-01T23:59:00'
    };
    if (args.length >= 3 &&
        typeof args[0] === 'string' &&
        typeof args[1] === 'string') {
        options.from = `1970-01-01T${args[0]}`;
        options.to = `1970-01-01T${args[1]}`;
        if (args.length === 4 && typeof args[2] === 'string') {
            format = args[2];
        }
    }
    return (0, date_fns_1.format)(faker_1.localFaker.date.between(options), format || 'HH:mm', {
        useAdditionalWeekYearTokens: true,
        useAdditionalDayOfYearTokens: true
    });
};
exports.default = time;
//# sourceMappingURL=time.js.map