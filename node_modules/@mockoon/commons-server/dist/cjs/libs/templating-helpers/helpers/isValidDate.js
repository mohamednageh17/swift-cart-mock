"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const date_fns_1 = require("date-fns");
const utils_1 = require("../../utils");
/**
 * Check if a date is valid
 *
 * @param args
 * @returns boolean
 */
const isValidDate = function (...args) {
    const parameters = args.slice(0, -1);
    if (parameters.length === 0) {
        return false;
    }
    // strings and number can be passed directly as a param, some helpers can return a SafeString or a Date object
    const date = (0, utils_1.fromSafeString)(parameters[0]);
    return (0, date_fns_1.isValid)((0, date_fns_1.toDate)(date));
};
exports.default = isValidDate;
//# sourceMappingURL=isValidDate.js.map