import { HelperOptions, SafeString } from 'handlebars';
declare const includes: (data: string | SafeString | HelperOptions, search: string | SafeString | HelperOptions | undefined) => boolean;
export default includes;
