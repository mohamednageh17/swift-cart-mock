"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../../utils");
const objectPath = function (...args) {
    var _a;
    // remove last item (handlebars options argument)
    const parameters = args.slice(0, -1);
    // we need at least some data and a path
    if (parameters.length < 2) {
        return;
    }
    const data = (0, utils_1.fromSafeString)(parameters[0]);
    const path = (0, utils_1.fromSafeString)(parameters[1]);
    const defaultValue = (_a = parameters[2]) !== null && _a !== void 0 ? _a : '';
    return utils_1.getValueFromPath.objectPath(data, path, defaultValue);
};
exports.default = objectPath;
//# sourceMappingURL=object-path.js.map