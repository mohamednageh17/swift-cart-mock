"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const commons_1 = require("@mockoon/commons");
const handlebars_1 = require("handlebars");
const oneOf = function (...args) {
    const parameters = args.slice(0, -1);
    let stringify = false;
    let itemList = [];
    if (parameters.length === 0 || !Array.isArray(parameters[0])) {
        return '';
    }
    if (parameters.length >= 1) {
        itemList = parameters[0] || [];
    }
    if (parameters.length >= 2) {
        stringify = parameters[1];
    }
    const result = itemList[(0, commons_1.RandomInt)(0, itemList.length - 1)];
    return stringify ? new handlebars_1.SafeString(JSON.stringify(result)) : result;
};
exports.default = oneOf;
//# sourceMappingURL=oneOf.js.map