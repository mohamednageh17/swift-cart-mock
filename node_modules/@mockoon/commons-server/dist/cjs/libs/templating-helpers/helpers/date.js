"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const date_fns_1 = require("date-fns");
const faker_1 = require("../../faker");
const utils_1 = require("../../utils");
const date = function (...args) {
    let format;
    const from = (0, utils_1.fromSafeString)(args[0]);
    const to = (0, utils_1.fromSafeString)(args[1]);
    if (args.length >= 3 && typeof from === 'string' && typeof to === 'string') {
        const randomDate = faker_1.localFaker.date.between({ from: from, to: to });
        if (args.length === 4 && typeof args[2] === 'string') {
            format = args[2];
            return (0, date_fns_1.format)(randomDate, format, {
                useAdditionalWeekYearTokens: true,
                useAdditionalDayOfYearTokens: true
            });
        }
        return randomDate.toString();
    }
    return '';
};
exports.default = date;
//# sourceMappingURL=date.js.map