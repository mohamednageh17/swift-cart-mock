"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResponseHelpers = exports.responseHelperNames = void 0;
const utils_1 = require("../utils");
exports.responseHelperNames = ['status'];
const ResponseHelpers = function (response) {
    return {
        // set status code
        status: function (...args) {
            // remove last item (handlebars options argument)
            const parameters = args.slice(0, -1);
            let code;
            if (parameters.length >= 1) {
                code = parseInt(parameters[0], 10);
                if ((0, utils_1.isValidStatusCode)(code)) {
                    response.locals.statusCode = code;
                }
                // Invalid status codes are silently ignored, letting the route's default status code be used
            }
            return '';
        }
    };
};
exports.ResponseHelpers = ResponseHelpers;
//# sourceMappingURL=response-helpers.js.map