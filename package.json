{
  "name": "swift-cart-mock",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx mockoon-cli start --data mockoon_env.json --port $PORT"
  },
  "dependencies": {
    "@mockoon/cli": "^9.5.0"
  }
}
